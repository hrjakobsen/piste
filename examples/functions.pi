extern piste_print(int) : int bound to print in
    fun add_1(param : int) : int =
        let added = param + 1
        in
            return added

    let a = 1,
        b : int = add_1(a),
        _ : int = print(b)
    in
        skip

